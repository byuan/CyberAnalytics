import React from 'react';

class Sources extends React.Component {
    render() {
        return (
            <p>Here we will list our sources, links to the source,
                 and maybe how/why we use the data</p>
        )
    }
}

export default Sources;